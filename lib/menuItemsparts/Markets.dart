import 'package:flutter/material.dart';

import '../rc.dart';

class Markets extends StatelessWidget {
  const Markets({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("markets"));
  }
}
