import 'package:flutter/material.dart';

import '../rc.dart';

class CA extends StatelessWidget {
  const CA({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("CA"));
  }
}
